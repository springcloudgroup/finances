spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native
  security:
    user:
#      password: ${CONFIG_SERVICE_PASSWORD}
      password: admin

server:
  port: 8888
