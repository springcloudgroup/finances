spring:
  application:
    name: account
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
      password: admin
      username: user
      name: account

